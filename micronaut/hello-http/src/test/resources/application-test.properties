# Bind the test server to loopback. Without this Micronaut advertises the machine's hostname,
# which on a host that resolves only to IPv6 link-local or VPN addresses leaves @Client("/")
# waiting on a socket nothing answers.
micronaut.server.host=localhost
