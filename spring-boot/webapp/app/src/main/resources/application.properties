spring.application.name=webapp
# Spring Boot's default static locations already include classpath:/static/, which is where the
# web module's jar puts the bundle; nothing to configure for the seam itself. Cache headers for the
# hashed assets live in spa/StaticAssets, so index.html itself is never cached.
