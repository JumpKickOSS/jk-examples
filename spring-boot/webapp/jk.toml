# A JVM backend serving a single-page front end: `app` is the Spring Boot service, `web` is a
# resource-only module that packages the Vite bundle as a jar with static/ at its root.

name    = "webapp"
group   = "com.example"
version = "0.1.0"
java    = 25

[workspace]
modules = ["app", "web"]

# Test tiers. Bare `jk test` is the unit tier; each profile owns exactly one of the excluded
# tags, so every tagged test runs in one tier and none is silently skipped.
[test]
exclude-tags = ["integration", "slow", "network", "bench"]

[profiles.integration]
include-tags = ["integration"]
exclude-tags = []

[profiles.network]
include-tags = ["network"]
exclude-tags = ["integration"]

[profiles.slow]
include-tags = ["slow"]
exclude-tags = ["integration", "network"]

[profiles.bench]
include-tags = ["bench"]
exclude-tags = ["integration", "network", "slow"]

[profiles.ci]
exclude-tags = ["integration", "slow", "network", "bench"]
