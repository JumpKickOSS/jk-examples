# jk import report

Source: `/home/bsant/src/scratch/maven-corpus/spring-cloud-alibaba/pom.xml`

## Tier 2 — imported with best-effort

These were mapped but you should review the result.

- `<parent>` was referenced (org.springframework.cloud:spring-cloud-build:5.0.2) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- Maven profile `release`: plugins=[maven-source-plugin,maven-javadoc-plugin,maven-gpg-plugin,central-publishing-maven-plugin] (plugin mapping is not yet implemented).
- `<plugin>maven-enforcer-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- `<plugin>maven-eclipse-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- `<plugin>maven-checkstyle-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- `<plugin>maven-surefire-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- `<plugin>flatten-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [spring-cloud-alibaba-dependencies] `<parent>` was referenced (org.springframework.cloud:spring-cloud-dependencies-parent:5.0.2) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.nacos:nacos-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-parameter-flow-control is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-datasource-extension is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-datasource-apollo is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-datasource-zookeeper is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-datasource-nacos is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-datasource-redis is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-datasource-consul is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-web-servlet is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-spring-cloud-gateway-v6x-adapter is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-transport-simple-http is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-annotation-aspectj is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-reactor-adapter is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-cluster-server-default is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-cluster-client-default is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-spring-webflux-adapter is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-api-gateway-adapter-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.csp:sentinel-spring-webmvc-v6x-adapter is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry org.apache.seata:seata-spring-boot-starter is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry org.apache.rocketmq:rocketmq-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry org.apache.rocketmq:rocketmq-acl is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry net.javacrumbs.shedlock:shedlock-spring is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry net.javacrumbs.shedlock:shedlock-provider-jdbc-template is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.aliyun.schedulerx:schedulerx2-worker is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-alibaba-sentinel-datasource is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-alibaba-sentinel-gateway is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-starter-alibaba-sentinel is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-circuitbreaker-sentinel is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-starter-alibaba-seata is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-alibaba-nacos-config is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-starter-alibaba-nacos-discovery is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-starter-alibaba-nacos-config is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-starter-stream-rocketmq is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-starter-bus-rocketmq is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-starter-alibaba-sidecar is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-alibaba-commons is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-starter-alibaba-schedulerx is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba:druid-spring-boot-4-starter is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba:druid is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry org.mybatis.spring.boot:mybatis-spring-boot-starter is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry org.apache.logging.log4j:log4j-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry org.slf4j:slf4j-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.alibaba:fastjson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry org.apache.logging.log4j:log4j-slf4j2-impl is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry com.google.guava:guava is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry org.projectlombok:lombok is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] `<dependencyManagement>` entry org.jspecify:jspecify is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-dependencies] Maven profile `release`: plugins=[maven-source-plugin,maven-javadoc-plugin,maven-gpg-plugin,flatten-maven-plugin,central-publishing-maven-plugin] (plugin mapping is not yet implemented).
- [spring-cloud-alibaba-examples] `<parent>` was referenced (com.alibaba.cloud:spring-cloud-alibaba:${revision}) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [spring-cloud-alibaba-examples] Maven profile `native`: contained no convertible payload; dropped.
- [spring-cloud-alibaba-examples] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [spring-cloud-alibaba-examples] `<plugin>maven-deploy-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [spring-cloud-alibaba-examples] `<plugin>native-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [spring-cloud-alibaba-examples] `<plugin>maven-javadoc-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [spring-cloud-alibaba-examples] `<plugin>maven-source-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [spring-cloud-alibaba-starters] `<parent>` was referenced (com.alibaba.cloud:spring-cloud-alibaba:${revision}) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [spring-cloud-alibaba-starters] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [spring-cloud-alibaba-starters] `<plugin>jacoco-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [spring-cloud-alibaba-coverage] `<parent>` was referenced (com.alibaba.cloud:spring-cloud-alibaba:${revision}) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [spring-cloud-alibaba-coverage] `<plugin>jacoco-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [spring-cloud-alibaba-coverage] `<plugin>maven-deploy-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [spring-cloud-alibaba-tests] `<parent>` was referenced (com.alibaba.cloud:spring-cloud-alibaba:${revision}) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [spring-cloud-alibaba-tests] `<dependencyManagement>` entry com.alibaba.cloud:spring-cloud-alibaba-test-support is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [spring-cloud-alibaba-tests] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [spring-cloud-alibaba-tests] `<plugin>maven-deploy-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.

