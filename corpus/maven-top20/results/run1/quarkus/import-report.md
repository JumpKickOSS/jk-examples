# jk import report

Source: `/home/bsant/src/scratch/maven-corpus/quarkus/pom.xml`

## Tier 3 — not imported

These constructs have no jk equivalent and were skipped or stubbed.

- `<build><extensions>` is not supported. Move build extensions to a custom jk task once tasks land.

## Tier 2 — imported with best-effort

These were mapped but you should review the result.

- `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- Maven profile `quick-build`: activation=property=quickly (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,skipDocs,enforcer.skip,maven-enforcer-plugin.phase,skipExtensionValidation,skip.gradle.tests,invoker.skip,jbang.skip,forbiddenapis.skip,forbiddenapis-maven-plugin.phase,skipCodestartValidation,truststore.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- Maven profile `quick-build-docs`: activation=property=quicklyDocs (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,skipExtensionValidation,skip.gradle.tests,invoker.skip,forbiddenapis.skip,skipCodestartValidation,truststore.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- Maven profile `quick-build-ci`: activation=property=quickly-ci (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,skipDocs,enforcer.skip,format.skip,skipExtensionValidation,skip.gradle.tests,invoker.skip,jbang.skip,forbiddenapis.skip,truststore.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- Maven profile `relocations`: contained no convertible payload; dropped.
- Maven profile `release`: contained no convertible payload; dropped.
- Maven profile `tcks`: activation=property=tcks (no jk equivalent — replace with an explicit jk profile or feature).
- Maven profile `incremental`: activation=property=incremental (no jk equivalent — replace with an explicit jk profile or feature).
- Maven profile `owasp-check`: activation=property=owasp-check (no jk equivalent — replace with an explicit jk profile or feature).
- Maven profile `owasp-report`: activation=property=owasp-report (no jk equivalent — replace with an explicit jk profile or feature).
- [independent-projects/parent] Maven profile `clean-cache`: activation=property=clean-cache (no jk equivalent — replace with an explicit jk profile or feature); plugins=[maven-clean-plugin] (plugin mapping is not yet implemented).
- [independent-projects/parent] Maven profile `release`: plugins=[maven-resources-plugin,maven-gpg-plugin,maven-jar-plugin,maven-source-plugin,maven-javadoc-plugin] (plugin mapping is not yet implemented).
- [independent-projects/parent] Maven profile `ci`: activation=property=env.CI (no jk equivalent — replace with an explicit jk profile or feature).
- [independent-projects/parent] `<plugin>maven-enforcer-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/parent] `<plugin>maven-source-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/parent] `<plugin>forbiddenapis</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/parent] `<plugin>module-services-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/ide-config] `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [independent-projects/ide-config] Maven profile `quick-build`: activation=property=quickly (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/ide-config] Maven profile `quick-build-docs`: activation=property=quicklyDocs (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/ide-config] Maven profile `quick-build-ci`: activation=property=quickly-ci (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,format.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/revapi] `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [independent-projects/revapi] Maven profile `quick-build`: activation=property=quickly (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/revapi] Maven profile `quick-build-docs`: activation=property=quicklyDocs (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/revapi] Maven profile `quick-build-ci`: activation=property=quickly-ci (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,format.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/revapi] `<plugin>maven-deploy-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/arc] `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [independent-projects/arc] `<dependencyManagement>` entry io.quarkus.arc:arc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry io.quarkus.arc:arc-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry io.quarkus.arc:arc-arquillian is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry io.quarkus.arc:arc-cdi-tck-porting-pkg is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry jakarta.enterprise:jakarta.enterprise.cdi-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry io.smallrye:jandex is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry io.smallrye:jandex-gizmo2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry io.quarkus.gizmo:gizmo is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry io.quarkus.gizmo:gizmo2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry org.jboss.logging:jboss-logging is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry jakarta.annotation:jakarta.annotation-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry jakarta.transaction:jakarta.transaction-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry jakarta.persistence:jakarta.persistence-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry org.assertj:assertj-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry io.quarkus.gizmo:gizmo is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] `<dependencyManagement>` entry org.vineflower:vineflower is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/arc] Maven profile `quick-build`: activation=property=quickly (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/arc] Maven profile `quick-build-docs`: activation=property=quicklyDocs (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/arc] Maven profile `quick-build-ci`: activation=property=quickly-ci (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,format.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/arc] Maven profile `format`: activation=activeByDefault+property=!no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/arc] Maven profile `validate`: activation=activeByDefault+property=no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/arc] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [independent-projects/bootstrap] `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [independent-projects/bootstrap] Maven profile `quick-build`: activation=property=quickly (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/bootstrap] Maven profile `quick-build-docs`: activation=property=quicklyDocs (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/bootstrap] Maven profile `quick-build-ci`: activation=property=quickly-ci (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,format.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/bootstrap] Maven profile `format`: activation=activeByDefault+property=!no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/bootstrap] Maven profile `validate`: activation=activeByDefault+property=no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/bootstrap] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [independent-projects/qute] `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [independent-projects/qute] `<dependencyManagement>` entry org.assertj:assertj-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/qute] `<dependencyManagement>` entry io.quarkus.qute:qute-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/qute] `<dependencyManagement>` entry io.quarkus.qute:qute-generator is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/qute] `<dependencyManagement>` entry io.quarkus.qute:qute-debug is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/qute] `<dependencyManagement>` entry org.jboss.logging:jboss-logging is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/qute] `<dependencyManagement>` entry io.smallrye:jandex is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/qute] `<dependencyManagement>` entry io.smallrye:jandex-gizmo2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/qute] `<dependencyManagement>` entry io.quarkus.gizmo:gizmo2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/qute] `<dependencyManagement>` entry org.eclipse.lsp4j:org.eclipse.lsp4j.debug is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/qute] Maven profile `quick-build`: activation=property=quickly (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/qute] Maven profile `quick-build-docs`: activation=property=quicklyDocs (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/qute] Maven profile `quick-build-ci`: activation=property=quickly-ci (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,format.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/qute] Maven profile `format`: activation=activeByDefault+property=!no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/qute] Maven profile `validate`: activation=activeByDefault+property=no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/qute] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [independent-projects/tools] `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [independent-projects/tools] `<dependencyManagement>` entry io.quarkus:quarkus-devtools-base-codestarts is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry io.quarkus:quarkus-devtools-testing is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry io.quarkus:quarkus-devtools-registry-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry org.apache.commons:commons-compress is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry io.quarkus.qute:qute-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry io.quarkus:quarkus-devtools-message-writer is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry io.quarkus:quarkus-devtools-codestarts is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry io.quarkus:quarkus-devtools-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry io.fabric8:maven-model-helper is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry jakarta.enterprise:jakarta.enterprise.cdi-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry javax.annotation:javax.annotation-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry javax.inject:javax.inject is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry org.assertj:assertj-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry org.jboss.logging:jboss-logging is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry org.codejive:java-properties is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry com.networknt:json-schema-validator is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] `<dependencyManagement>` entry io.quarkus:quarkus-bootstrap-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/tools] Maven profile `quick-build`: activation=property=quickly (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/tools] Maven profile `quick-build-docs`: activation=property=quicklyDocs (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/tools] Maven profile `quick-build-ci`: activation=property=quickly-ci (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,format.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/tools] Maven profile `format`: activation=activeByDefault+property=!no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/tools] Maven profile `validate`: activation=activeByDefault+property=no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/tools] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [independent-projects/tools] `<plugin>maven-enforcer-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/tools] `<plugin>maven-surefire-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/enforcer-rules] `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [independent-projects/enforcer-rules] `<exclusions>` on org.apache.maven:maven-core — exclusion support lands in a later slice; exclusions were dropped.
- [independent-projects/enforcer-rules] Maven profile `quick-build`: activation=property=quickly (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,invoker.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/enforcer-rules] Maven profile `quick-build-docs`: activation=property=quicklyDocs (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,invoker.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/enforcer-rules] Maven profile `quick-build-ci`: activation=property=quickly-ci (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,format.skip,invoker.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/enforcer-rules] Maven profile `format`: activation=activeByDefault+property=!no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/enforcer-rules] Maven profile `validate`: activation=activeByDefault+property=no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/enforcer-rules] `<plugin>sisu-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/enforcer-rules] `<plugin>maven-invoker-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/vertx-utils] `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [independent-projects/resteasy-reactive] `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-build-support is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-common-types is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-common-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.vertx.utils:quarkus-vertx-utils is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-client-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-jsonb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-jackson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry jakarta.enterprise:jakarta.enterprise.cdi-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry jakarta.json:jakarta.json-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.smallrye:jandex is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry net.bytebuddy:byte-buddy is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry net.bytebuddy:byte-buddy-maven-plugin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.quarkus.gizmo:gizmo is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.eclipse.yasson:yasson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.eclipse.parsson:parsson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry jakarta.json.bind:jakarta.json.bind-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.vertx:vertx-web is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.vertx:vertx-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.vertx:vertx-web-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.reactivestreams:reactive-streams is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.smallrye.reactive:mutiny-zero-flow-adapters is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.smallrye.reactive:smallrye-mutiny-vertx-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.jboss.shrinkwrap:shrinkwrap-depchain is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.apache.maven:maven-plugin-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.apache.maven.plugin-tools:maven-plugin-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.apache.maven:maven-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.jboss.logging:jboss-logging is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.jboss.logmanager:jboss-logmanager is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry jakarta.annotation:jakarta.annotation-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry jakarta.persistence:jakarta.persistence-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.assertj:assertj-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.awaitility:awaitility is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.wiremock:wiremock-standalone is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry io.rest-assured:rest-assured is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry org.jboss.logging:commons-logging-jboss-logging is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry jakarta.validation:jakarta.validation-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry jakarta.ws.rs:jakarta.ws.rs-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] `<dependencyManagement>` entry jakarta.xml.bind:jakarta.xml.bind-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [independent-projects/resteasy-reactive] Maven profile `quick-build`: activation=property=quickly (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/resteasy-reactive] Maven profile `quick-build-docs`: activation=property=quicklyDocs (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/resteasy-reactive] Maven profile `quick-build-ci`: activation=property=quickly-ci (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,format.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/resteasy-reactive] Maven profile `format`: activation=activeByDefault+property=!no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/resteasy-reactive] Maven profile `validate`: activation=activeByDefault+property=no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/resteasy-reactive] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [independent-projects/extension-maven-plugin] `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [independent-projects/extension-maven-plugin] `<dependency>` io.quarkus:quarkus-bootstrap-core has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<exclusions>` on io.quarkus:quarkus-bootstrap-maven-resolver — exclusion support lands in a later slice; exclusions were dropped.
- [independent-projects/extension-maven-plugin] `<dependency>` io.quarkus:quarkus-bootstrap-maven-resolver has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<exclusions>` on io.quarkus:quarkus-devtools-common — exclusion support lands in a later slice; exclusions were dropped.
- [independent-projects/extension-maven-plugin] `<dependency>` javax.inject:javax.inject has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven:maven-plugin-api has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven.plugin-tools:maven-plugin-annotations has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven:maven-core has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven:maven-embedder has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven:maven-settings-builder has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven:maven-resolver-provider has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven:maven-model has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven:maven-model-builder has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven:maven-artifact has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven:maven-settings has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven:maven-builder-support has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven:maven-repository-metadata has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` commons-io:commons-io has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` tools.jackson.core:jackson-databind has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` tools.jackson.dataformat:jackson-dataformat-yaml has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` jakarta.inject:jakarta.inject-api has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` io.smallrye.beanbag:smallrye-beanbag has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<exclusions>` on io.smallrye.beanbag:smallrye-beanbag-maven — exclusion support lands in a later slice; exclusions were dropped.
- [independent-projects/extension-maven-plugin] `<dependency>` io.smallrye.beanbag:smallrye-beanbag-maven has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<exclusions>` on io.smallrye.beanbag:smallrye-beanbag-sisu — exclusion support lands in a later slice; exclusions were dropped.
- [independent-projects/extension-maven-plugin] `<dependency>` io.smallrye.beanbag:smallrye-beanbag-sisu has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.junit.jupiter:junit-jupiter has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.apache.maven.plugin-testing:maven-plugin-testing-harness has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<dependency>` org.mockito:mockito-core has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/extension-maven-plugin] `<exclusions>` on org.apache.maven.shared:maven-invoker — exclusion support lands in a later slice; exclusions were dropped.
- [independent-projects/extension-maven-plugin] Maven profile `quick-build`: activation=property=quickly (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/extension-maven-plugin] Maven profile `quick-build-docs`: activation=property=quicklyDocs (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/extension-maven-plugin] Maven profile `quick-build-ci`: activation=property=quickly-ci (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,format.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/extension-maven-plugin] Maven profile `format`: activation=activeByDefault+property=!no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/extension-maven-plugin] Maven profile `validate`: activation=activeByDefault+property=no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/extension-maven-plugin] `<plugin>sisu-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/extension-maven-plugin] `<plugin>maven-plugin-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/extension-maven-plugin] `<plugin>maven-resources-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [independent-projects/junit-virtual-threads] `<parent>` was referenced (io.quarkus:quarkus-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [independent-projects/junit-virtual-threads] `<dependency>` org.junit.jupiter:junit-jupiter has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/junit-virtual-threads] `<dependency>` org.junit.platform:junit-platform-testkit has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/junit-virtual-threads] `<dependency>` io.smallrye.common:smallrye-common-annotation has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [independent-projects/junit-virtual-threads] Maven profile `quick-build`: activation=property=quickly (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/junit-virtual-threads] Maven profile `quick-build-docs`: activation=property=quicklyDocs (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/junit-virtual-threads] Maven profile `quick-build-ci`: activation=property=quickly-ci (no jk equivalent — replace with an explicit jk profile or feature); properties=[skipTests,skipITs,enforcer.skip,format.skip] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [independent-projects/junit-virtual-threads] Maven profile `format`: activation=activeByDefault+property=!no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [independent-projects/junit-virtual-threads] Maven profile `validate`: activation=activeByDefault+property=no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [bom/application] `<exclusions>` on io.quarkus:quarkus-bootstrap-bom — exclusion support lands in a later slice; exclusions were dropped.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-bom-quarkus-platform-descriptor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-bom-quarkus-platform-properties is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.netty:netty-tcnative-classes is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-openssl is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-openssl is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.aayushatharva.brotli4j:brotli4j is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.aayushatharva.brotli4j:native-linux-x86_64 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.aayushatharva.brotli4j:native-linux-s390x is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.aayushatharva.brotli4j:native-linux-ppc64le is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.aayushatharva.brotli4j:native-linux-aarch64 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.aayushatharva.brotli4j:native-linux-armv7 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.aayushatharva.brotli4j:native-linux-riscv64 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.aayushatharva.brotli4j:native-windows-x86_64 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.aayushatharva.brotli4j:native-windows-aarch64 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.aayushatharva.brotli4j:native-osx-x86_64 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.aayushatharva.brotli4j:native-osx-aarch64 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.certs:smallrye-certificate-generator is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.certs:smallrye-private-key-pem-parser is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.vertx:vertx-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.perfmark:perfmark-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.testcontainers:testcontainers is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.opensearch:opensearch-testcontainers is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.search:hibernate-search-backend-elasticsearch is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jctools:jctools-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jetbrains:annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jetbrains.kotlinx:kotlinx-metadata-jvm is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-core-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-development-mode-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-class-change-agent is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-ide-launcher is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-arc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-arc-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-arc-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-signals is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-signals-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-signals-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-assistant is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-assistant-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-assistant-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-assistant-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-caffeine is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-caffeine-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devjsonrpc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devjsonrpc-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devjsonrpc-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devmcp is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devmcp-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devmcp-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devmcp-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devui is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devui-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devui-resources is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devui-test-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devui-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-extension-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jacoco is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jacoco-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jaxb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jaxb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jaxp is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jaxp-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jackson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jackson-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jackson-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jsonb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jsonb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jsonb-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jsonp is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jsonp-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hal is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hal-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-kotlin-serialization is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-kotlin-serialization-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-netty is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-netty-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-tls-registry is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-tls-registry-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-tls-registry-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-tls-registry-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-proxy-registry is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-proxy-registry-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-agroal is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-agroal-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-agroal-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-agroal-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-config-yaml is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-config-yaml-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cyclonedx is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cyclonedx-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cyclonedx-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cyclonedx-generator is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cyclonedx-endpoint is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cyclonedx-endpoint-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-datasource-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-datasource-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-datasource is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-datasource-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-datasource-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-postgresql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-h2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-mysql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-mariadb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-db2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-mssql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-oracle is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-datasource-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elasticsearch-rest-client-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elasticsearch-rest-client-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elasticsearch-rest-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elasticsearch-rest-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elasticsearch-java-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elasticsearch-java-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security-runtime-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security-properties-file is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security-properties-file-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security-oauth2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security-oauth2-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-csrf is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-csrf-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-oidc-server is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-keycloak-server is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-client-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-oidc-filter is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-oidc-filter-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-oidc-filter is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-oidc-filter-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-client-registration is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-client-registration-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spiffe-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spiffe-client-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spiffe-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-client-graphql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-client-graphql-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-token-propagation-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-token-propagation-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-oidc-token-propagation is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-oidc-token-propagation-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-db-token-state-manager is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-db-token-state-manager-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-redis-token-state-manager is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-redis-token-state-manager-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-oidc-token-propagation is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-oidc-token-propagation-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-oidc-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-keycloak-authorization is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-keycloak-authorization-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-keycloak-admin-resteasy-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-keycloak-admin-resteasy-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-keycloak-admin-rest-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-keycloak-admin-rest-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-keycloak-admin-client-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-keycloak-admin-client-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-keycloak is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devservices-oidc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-postgresql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-postgresql-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-oracle is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-oracle-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-mysql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-mysql-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-mssql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-mssql-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-db2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-flyway-db2-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-liquibase-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-liquibase is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-liquibase-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-liquibase-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-liquibase-mongodb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-liquibase-mongodb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-envers is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-envers-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-reactive is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-reactive-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-transactions is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-transactions-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-panache-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-panache-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-panache-hibernate-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-panache-hibernate-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-panache-mock is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-panacheql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-data-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-data-hibernate is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-data-hibernate-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm-panache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm-panache-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-reactive-panache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-reactive-panache-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-reactive-panache-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-reactive-panache-kotlin-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm-panache-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm-panache-kotlin-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm-panache-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm-panache-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-reactive-panache-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-reactive-panache-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-reactive-rest-data-panache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-reactive-rest-data-panache-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-data-panache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-data-panache-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm-rest-data-panache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-orm-rest-data-panache-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mongodb-rest-data-panache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mongodb-rest-data-panache-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mongodb-panache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mongodb-panache-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mongodb-panache-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mongodb-panache-kotlin-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mongodb-panache-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mongodb-panache-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-search-backend-elasticsearch-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-search-backend-elasticsearch-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-search-orm-elasticsearch is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-search-orm-elasticsearch-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-search-orm-elasticsearch-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-search-orm-outbox-polling is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-search-orm-outbox-polling-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-search-standalone-elasticsearch is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-search-standalone-elasticsearch-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-search-standalone-elasticsearch-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-validator is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-validator-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-hibernate-validator-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-infinispan-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-infinispan-client-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-infinispan-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-infinispan-cache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-infinispan-cache-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-db2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-db2-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-postgresql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-postgresql-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-h2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-h2-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-mariadb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-mariadb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-mssql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-mssql-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-mysql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-mysql-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-oracle is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jdbc-oracle-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-modular is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-modular-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-modular-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jlink is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jlink-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jlink-launcher is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jlink-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jms-spi-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kafka-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kafka-client-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kafka-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kafka-streams is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kafka-streams-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kafka-streams-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-kafka-companion is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-avro is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-avro-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-avro-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-apicurio-registry-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-apicurio-registry-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-apicurio-registry-avro is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-apicurio-registry-avro-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-apicurio-registry-json-schema is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-apicurio-registry-json-schema-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-confluent-registry-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-confluent-registry-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-confluent-registry-avro is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-confluent-registry-json-schema is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-confluent-registry-json-schema-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-confluent-registry-avro-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-schema-registry-devservice is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-schema-registry-devservice-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-health is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-health-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-health-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-jwt is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-jwt-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-jwt-build is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-jwt-build-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-context-propagation is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-context-propagation-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-context-propagation-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-reactive-streams-operators is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-reactive-streams-operators-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mutiny-reactive-streams-operators is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mutiny-reactive-streams-operators-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-reactive-type-converters is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-reactive-type-converters-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-kafka is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-kafka-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-pulsar is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-pulsar-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-amqp is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-amqp-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-mqtt is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-mqtt-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-rabbitmq is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-rabbitmq-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-messaging-rabbitmq-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-openapi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-openapi-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-openapi-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-openapi-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-graphql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-graphql-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-graphql-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-graphql-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-graphql-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-stork is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-stork-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-config is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-config-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-jackson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-jackson-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-jaxb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-jaxb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-jsonb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-jsonb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-mutiny is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-client-mutiny-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-jackson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-jackson-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-jsonb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-jsonb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-jaxb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-jaxb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-links is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-links-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-multipart is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-multipart-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-server-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-server-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-transaction-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-narayana-jta is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-narayana-jta-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-narayana-lra is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-narayana-lra-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-undertow is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-undertow-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-fault-tolerance is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-fault-tolerance-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-fault-tolerance-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-health-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-smallrye-openapi-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-load-shedding is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-load-shedding-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-vertx is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-vertx-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-vertx-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-vertx-latebound-mdc-provider is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-vertx-http is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-vertx-http-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-vertx-http-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-vertx-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-vertx-kotlin-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-routes is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-routes-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-datasource-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-datasource-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-db2-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-db2-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-pg-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-pg-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-mysql-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-mysql-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-mssql-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-mssql-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-oracle-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-oracle-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mailer is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mailer-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mongodb-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mongodb-client-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mongodb-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-mongodb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-grpc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-grpc-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-grpc-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-grpc-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-grpc-codegen is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-grpc-stubs is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-grpc-reflection is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-grpc-cli is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-grpc-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-grpc-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-grpc-protoc-plugin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-websockets is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-websockets-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-websockets-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-websockets-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-websockets-next is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-websockets-next-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-websockets-next-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-websockets-next-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-websockets-next-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-http3 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-http3-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-http3-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-undertow-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-scheduler-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-scheduler-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-scheduler-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-scheduler-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-scheduler is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-scheduler-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-scheduler-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-quartz is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-quartz-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-di is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-di-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-security is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-security-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-scheduled is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-scheduled-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-web-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-web is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-web-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-web-resteasy is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-web-resteasy-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-web-rest is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-web-rest-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-data-jpa is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-data-jpa-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-data-rest is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-data-rest-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-boot-properties is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-boot-properties-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-cloud-config-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-cloud-config-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-cache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-cache-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-tx is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-tx-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-swagger-ui is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-swagger-ui-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kotlin-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-funqy-amazon-lambda is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-funqy-amazon-lambda-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-funqy-google-cloud-functions is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-funqy-google-cloud-functions-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-funqy-knative-events is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-funqy-knative-events-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-funqy-http is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-funqy-http-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-funqy-server-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-funqy-server-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-xray is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-xray-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-http is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-http-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-rest is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-rest-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-event-server is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-http-event-server is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-amazon-lambda-rest-event-server is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-data-hibernate-json-types is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-data-hibernate-json-types-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-kotlin-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-spi-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-server-spi-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-servlet-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-servlet is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-jaxrs is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-jaxrs-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-spi-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-jackson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-jackson-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-jaxb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-jaxb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-jsonb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-jsonb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-kotlin-serialization-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-kotlin-serialization-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-kotlin-serialization is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-client-kotlin-serialization-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-qute is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-qute-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-jsonb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-jsonb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-jsonb-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-jsonb-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-jaxb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-jaxb-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-jackson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-jackson-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-jackson-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-jackson-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-links is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-rest-links-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-datasource is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-azure-functions is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-azure-functions-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-azure-functions-http is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-azure-functions-http-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-buildpack is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-buildpack-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-docker is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-docker-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-podman is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-podman-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-jib is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-jib-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-openshift is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-openshift-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-docker-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-docker-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-container-image-util is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-openshift is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-openshift-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-minikube is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-minikube-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kind is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kind-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-client-internal is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-config is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-config-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-client-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-client-internal-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-service-binding is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-service-binding-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-kubernetes-service-binding-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-openshift-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-openshift-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-narayana-stm is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-narayana-stm-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security-jdbc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security-jdbc-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security-jpa is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security-jpa-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security-jpa-reactive is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security-jpa-reactive-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security-jpa-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security-jpa-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security-webauthn is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security-webauthn-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security-ldap is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-elytron-security-ldap-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-credentials is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-credentials-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-logging-json is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-logging-json-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-logging-gelf is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-logging-gelf-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cache-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cache-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cache-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cache-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cache-kotlin-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-google-cloud-functions is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-google-cloud-functions-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-google-cloud-functions-http is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-google-cloud-functions-http-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-google-cloud-functions is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-micrometer-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-micrometer is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-micrometer-opentelemetry-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-micrometer-opentelemetry is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-micrometer-registry-prometheus-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-micrometer-registry-prometheus is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-opentelemetry-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-opentelemetry is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-observation-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-observation is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-info-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-info-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-info-runtime-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-info is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-virtual-threads is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-virtual-threads-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-observability-devservices-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-observability-devservices is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-observability-devservices-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-observability-devservices-lgtm is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-observability-testcontainers is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-observability-devresource-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-observability-devresource-testcontainers is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-observability-devresource-lgtm is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-junit is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-junit-config is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-junit-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-junit-component is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-junit-internal is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-junit-mockito is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-junit-mockito-config is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-aesh is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-security is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-security-jwt is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-security-oidc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-security-webauthn is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-spring is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-vertx is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-hibernate-reactive-panache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-arquillian is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-h2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-ldap is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-test-kubernetes-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-qute is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-qute-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-qute is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-qute-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-aesh is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-aesh-dev is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-aesh-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-aesh-websocket is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-aesh-websocket-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-aesh-ssh is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-aesh-ssh-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-picocli is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-picocli-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-web-dependency-locator is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-web-dependency-locator-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-project-core-extension-codestarts is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.junit:junit-virtual-threads is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry net.bytebuddy:byte-buddy is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry net.bytebuddy:byte-buddy-agent is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.geolatte:geolatte-geom is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.api.grpc:proto-google-common-protos is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.code.findbugs:jsr305 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.bouncycastle:bc-fips is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.bouncycastle:bctls-fips is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:kubernetes-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:openshift-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:knative-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:servicebinding-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:helm-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:certmanager-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:kind-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:prometheus-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:s2i-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:docker-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:option-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.dekorate:dekorate-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.logging:jboss-logging is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.logging:commons-logging-jboss-logging is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.logmanager:jboss-logmanager is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.logmanager:log4j2-jboss-logmanager is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.logging.log4j:log4j-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.gizmo:gizmo is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.gizmo:gizmo is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.gizmo:gizmo2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.mvnpm:importmap is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry biz.paluch.logging:logstash-gelf is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.commons:commons-lang3 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.commons:commons-text is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry commons-codec:commons-codec is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.github.ben-manes.caffeine:caffeine is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.cloud.functions:functions-framework-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.code.gson:gson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.fasterxml:classmate is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.angus:angus-activation is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.angus:angus-mail is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.h2database:h2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.agroal:agroal-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.agroal:agroal-narayana is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.agroal:agroal-pool is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.apicurio:apicurio-registry-java-sdk is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.apicurio:apicurio-registry-avro-serde-kafka is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.apicurio:apicurio-registry-jsonschema-serde-kafka is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.apicurio:apicurio-registry-serde-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.apicurio:apicurio-registry-serde-kafka-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.apicurio:apicurio-registry-serde-common-jsonschema is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mutiny is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-mutiny-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-mutiny is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-mutiny-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-mutiny-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-mutiny-common-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-server-common-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-resteasy-common-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.reactivex.rxjava2:rxjava is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.rest-assured:rest-assured is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.rest-assured:json-schema-validator is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.rest-assured:kotlin-extensions is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hamcrest:hamcrest is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.antlr:antlr4-runtime is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.antlr:antlr4 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.scala-lang:scala-reflect is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.scala-lang:scala-library is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.http:quarkus-http-vertx-backend is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.http:quarkus-http-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.http:quarkus-http-http-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.http:quarkus-http-servlet is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.http:quarkus-http-websocket-vertx is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.http:quarkus-http-websocket-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.security:quarkus-security is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-security-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jaxrs-spi-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.config:smallrye-config is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-health is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-health-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-health-ui is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-health-provided-checks is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.modules:smallrye-modules is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-open-api-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-open-api-jaxrs is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-open-api-spring is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-open-api-vertx is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-open-api-ui is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.graphql-java:graphql-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql-schema-model is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql-cdi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql-schema-builder is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql-ui-graphiql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql-client-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql-client-implementation-vertx is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql-client-model-builder is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql-client-model is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-graphql-jackson-jsonb-compat is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-context-propagation is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-context-propagation-propagators-rxjava1 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-context-propagation-propagators-rxjava2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-context-propagation-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-context-propagation-jta is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-context-propagation-storage is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-jwt is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-jwt-build is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:smallrye-jwt-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.activation:jakarta.activation-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.inject:jakarta.inject-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.annotation:jakarta.annotation-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.authentication:jakarta.authentication-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.authorization:jakarta.authorization-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.el:jakarta.el-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.mail:jakarta.mail-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.persistence:jakarta.persistence-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.data:jakarta.data-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.resource:jakarta.resource-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.servlet:jakarta.servlet-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.transaction:jakarta.transaction-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.validation:jakarta.validation-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.interceptor:jakarta.interceptor-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.json:jakarta.json-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.qpid:proton-j is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents.client5:httpclient5-cache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents.client5:httpclient5-fluent is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents.client5:httpclient5 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents.core5:httpcore5-h2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents.core5:httpcore5-reactive is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents.core5:httpcore5 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents:httpclient is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents:httpclient-cache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents:httpmime is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents:httpasyncclient is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents:httpcore is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.httpcomponents:httpcore-nio is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.kafka:kafka-clients is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry at.yawk.lz4:lz4-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.xerial.snappy:snappy-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.avro:avro is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.avro:avro-compiler is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.avro:avro-idl is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.kafka:kafka-streams is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.kafka:kafka-streams-test-utils is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.kafka:kafka_2.13 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.strimzi:kafka-oauth-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.strimzi:kafka-oauth-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.strimzi:strimzi-test-container is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.nimbusds:nimbus-jose-jwt is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.bitbucket.b_c:jose4j is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.maven:maven-toolchain is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.mongodb:mongodb-driver-sync is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.mongodb:mongodb-driver-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.mongodb:mongodb-driver-legacy is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.mongodb:mongodb-driver-reactivestreams is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.mongodb:bson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.mongodb:mongodb-crypt is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry net.java.dev.jna:jna is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry net.java.dev.jna:jna-platform is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.codehaus.plexus:plexus-component-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.microprofile.config:microprofile-config-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.microprofile.health:microprofile-health-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.microprofile.context-propagation:microprofile-context-propagation-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.microprofile.fault-tolerance:microprofile-fault-tolerance-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.microprofile.rest.client:microprofile-rest-client-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.parsson:parsson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.yasson:yasson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.microprofile.jwt:microprofile-jwt-auth-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.microprofile.openapi:microprofile-openapi-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.glassfish.expressly:expressly is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.json.bind:jakarta.json.bind-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.glassfish.jaxb:jaxb-runtime is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.glassfish.jaxb:jaxb-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.glassfish.jaxb:txw2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.xml.bind:jakarta.xml.bind-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry javax.annotation:javax.annotation-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry javax.inject:javax.inject is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.websocket:jakarta.websocket-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.ws.rs:jakarta.ws.rs-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.graalvm.sdk:graal-sdk is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.graalvm.sdk:nativeimage is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.graalvm.polyglot:js-community is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.graalvm.polyglot:js is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:jandex is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye:jandex-gizmo2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.logging:jboss-logging-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.logging:jboss-logging-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.metadata:jboss-metadata-web is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.narayana.jta:narayana-jta is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.narayana.jts:narayana-jts-integration is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.narayana.stm:stm is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.narayana.lra:lra-coordinator-jar is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.microprofile.lra:microprofile-lra-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.narayana.lra:narayana-lra is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.narayana.lra:lra-service-base is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.narayana.lra:lra-proxy-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.narayana.lra:lra-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.resteasy:resteasy-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.resteasy:resteasy-core-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.resteasy.microprofile:microprofile-rest-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.resteasy:resteasy-json-binding-provider is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.resteasy:resteasy-json-p-provider is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.resteasy:resteasy-jaxb-provider is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.resteasy:resteasy-jackson2-provider is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry dev.resteasy.providers:resteasy-jackson-provider is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.resteasy:resteasy-rxjava2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.resteasy.spring:resteasy-spring-web is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.resteasy.microprofile:microprofile-config is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.resteasy:resteasy-links is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.shrinkwrap:shrinkwrap-depchain is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.marshalling:jboss-marshalling is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.threads:jboss-threads is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss:jboss-transaction-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.arc:arc-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.arc:arc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-vertx is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-common-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-common-types is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-jsonb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-jackson is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.resteasy.reactive:resteasy-reactive-client-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.vertx.utils:quarkus-vertx-utils is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.common:wildfly-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.cyclonedx:cyclonedx-core-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.openssl:wildfly-openssl-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.openssl:wildfly-openssl-linux-x86_64 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.client:wildfly-client-config is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-ssh-util is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-auth-server is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-password-impl is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-realm is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-realm-token is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-realm-jdbc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-realm-ldap is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-ssl is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-sasl-plain is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-sasl-digest is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-sasl-external is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-sasl-oauth2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-sasl-scram is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-x500-cert is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-x500-cert-acme is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-credential is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.sshd:sshd-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.sshd:sshd-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-sasl-gs2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-asn1 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-sasl-gssapi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-security-manager-action is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-auth is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-base is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-http is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-keystore is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-mechanism-digest is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-mechanism-gssapi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-mechanism-oauth2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-mechanism-scram is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-mechanism is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-permission is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-provider-util is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-sasl is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-util is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-x500-cert-util is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.wildfly.security:wildfly-elytron-x500 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.ow2.asm:asm is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.ow2.asm:asm-commons is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.ow2.asm:asm-tree is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.ow2.asm:asm-analysis is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.ow2.asm:asm-util is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jboss.slf4j:slf4j-jboss-logmanager is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry jakarta.enterprise:jakarta.enterprise.cdi-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry commons-io:commons-io is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.validator:hibernate-validator is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.orm:hibernate-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.orm:hibernate-graalvm is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.orm:hibernate-envers is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.orm:hibernate-spatial is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.orm:hibernate-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.orm:hibernate-jpamodelgen is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.orm:hibernate-assistant is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate:hibernate-jpamodelgen is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.models:hibernate-models is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.orm:hibernate-community-dialects is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.orm:hibernate-vector is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.reactive:hibernate-reactive-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.orm:hibernate-jcache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.github.ben-manes.caffeine:jcache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.search:hibernate-search-mapper-orm is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.search:hibernate-search-engine is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.search:hibernate-search-mapper-pojo-base is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hibernate.search:hibernate-search-util-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.postgresql:postgresql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.mariadb.jdbc:mariadb-java-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.mysql:mysql-connector-j is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.ibm.db2:jcc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.microsoft.sqlserver:mssql-jdbc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry co.elastic.clients:elasticsearch-rest5-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry co.elastic.clients:elasticsearch-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jacoco:org.jacoco.core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jacoco:org.jacoco.report is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jacoco:org.jacoco.agent is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jacoco:org.jacoco.agent is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.reactive:smallrye-mutiny-vertx-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.ongres.scram:scram-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.cronutils:cron-utils is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.quartz-scheduler:quartz is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.reactive:smallrye-reactive-messaging-kafka is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.pulsar:pulsar-client-original is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.pulsar:pulsar-client-messagecrypto-bc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.asynchttpclient:async-http-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.reactive:smallrye-reactive-messaging-kafka-test-companion is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.reactive:smallrye-reactive-messaging-amqp is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.infinispan:infinispan-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.infinispan:infinispan-counter-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.infinispan:infinispan-client-hotrod is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.infinispan:infinispan-commons is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.infinispan:infinispan-query is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.infinispan:testcontainers-infinispan is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.infinispan.protostream:protostream-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.infinispan.protostream:protostream-types is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.infinispan.protostream:protostream is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.infinispan:infinispan-component-annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.reactivestreams:reactive-streams is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.reactive:mutiny-zero is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.reactive:mutiny-zero-flow-adapters is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.microprofile.reactive-streams-operators:microprofile-reactive-streams-operators-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.eclipse.microprofile.reactive-streams-operators:microprofile-reactive-streams-operators-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.reactive:smallrye-reactive-streams-operators is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.reactive:smallrye-reactive-converter-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.reactive:smallrye-reactive-converter-mutiny is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.smallrye.reactive:smallrye-reactive-converter-rxjava2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jetbrains.kotlinx:kotlinx-serialization-json is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.amazonaws:aws-lambda-java-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.amazonaws:aws-lambda-java-events is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.amazonaws:aws-xray-recorder-sdk-aws-sdk-v2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.cloudevents:cloudevents-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.microsoft.azure.functions:azure-functions-java-library is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.microsoft.azure.functions:azure-functions-java-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.microsoft.azure:azure-toolkit-common-lib is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.microsoft.azure:azure-toolkit-auth-lib is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.microsoft.azure:azure-toolkit-appservice-lib is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.microsoft.azure:azure-toolkit-applicationinsights-lib is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.maven.shared:maven-invoker is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.awaitility:awaitility is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.awaitility:awaitility-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry de.flapdoodle.embed:de.flapdoodle.embed.mongo is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.flywaydb:flyway-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.flywaydb:flyway-sqlserver is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.flywaydb:flyway-mysql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.flywaydb:flyway-database-oracle is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.flywaydb:flyway-database-postgresql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.flywaydb:flyway-database-db2 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.flywaydb:flyway-database-mongodb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.liquibase:liquibase-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.osgi:osgi.core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.liquibase.ext:liquibase-mongodb is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.yaml:snakeyaml is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.checkerframework:checker-qual is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.errorprone:error_prone_annotations is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-core-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-context-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-beans-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-aop-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-data-jpa-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-boot-orm-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-data-commons-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-data-rest-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-web-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-webmvc-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-security-core-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-boot-properties-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-spring-boot-test-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.keycloak:keycloak-admin-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.keycloak:keycloak-authz-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.keycloak:keycloak-policy-enforcer is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.keycloak:keycloak-client-common-synced is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-vertx-graphql is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-vertx-graphql-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-redis-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-redis-cache is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-redis-client-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-redis-cache-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.qute:qute-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus.qute:qute-generator is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry dev.snowdrop:buildpack-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.cloud.tools:jib-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.salesforce.servicelibs:jprotoc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protobuf-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protobuf-java-util is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protobuf-kotlin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protoc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protoc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protoc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protoc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protoc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protoc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protoc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protoc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.google.protobuf:protoc is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.grpc:protoc-gen-grpc-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.grpc:protoc-gen-grpc-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.grpc:protoc-gen-grpc-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.grpc:protoc-gen-grpc-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.grpc:protoc-gen-grpc-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.grpc:protoc-gen-grpc-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.grpc:protoc-gen-grpc-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.grpc:protoc-gen-grpc-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.grpc:protoc-gen-grpc-java is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.github.javaparser:javaparser-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.aesh:readline is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jline:jline is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jline:jansi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.aesh:aesh is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.aesh:aesh-processor is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.aesh:terminal-http is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.aesh:terminal-ssh is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.jsoup:jsoup is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.projectreactor:reactor-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.github.luben:zstd-jni is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.sshd:sshd-netty is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry info.picocli:picocli is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry info.picocli:picocli-codegen is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.hdrhistogram:HdrHistogram is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.latencyutils:LatencyUtils is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.commons:commons-compress is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-builder is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devtools-registry-client is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devtools-testing is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-devtools-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-cli-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-analytics-common is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-apache-httpclient is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-apache-httpclient-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-awt is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-awt-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.crac:crac is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.dajudge.kindcontainer:kindcontainer is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.james:apache-mime4j-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.james:apache-mime4j-dom is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry org.apache.james:apache-mime4j-storage is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.opentelemetry.semconv:opentelemetry-semconv-incubating is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jfr-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jfr is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jfr-deployment-spi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry io.quarkus:quarkus-jfr-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.webauthn4j:webauthn4j-core-async is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] `<dependencyManagement>` entry com.webauthn4j:webauthn4j-metadata-async is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/application] dependency handle `jackson-bom` collides in [platform-dependencies]; com.fasterxml.jackson:jackson-bom was written as `jackson-bom-2`.
- [bom/application] `<plugin>quarkus-platform-bom-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [bom/application] `<plugin>maven-enforcer-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [bom/test] `<exclusions>` on io.quarkus:quarkus-bom — exclusion support lands in a later slice; exclusions were dropped.
- [bom/test] `<dependencyManagement>` entry io.reactivex:rxjava is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/test] `<dependencyManagement>` entry io.smallrye.certs:smallrye-certificate-generator-junit5 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/test] `<dependencyManagement>` entry io.strimzi:strimzi-test-container is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/test] `<dependencyManagement>` entry javax.xml.bind:jaxb-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/test] `<dependencyManagement>` entry io.opentelemetry.proto:opentelemetry-proto is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.vaadin:router is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:path-to-regexp is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:type-fest is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:tagged-tag is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.mvnpm:vaadin-webcomponents is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.vaadin:vaadin-usage-statistics is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.vaadin:vaadin-development-mode-detector is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.polymer:polymer is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.webcomponents:shadycss is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.open-wc:dedupe-mixin is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:style-observer is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:marked is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:dompurify is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:lit is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.lit:reactive-element is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:lit-element is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:lit-html is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.lit-labs:ssr-dom-shim is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.lit:localize is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:flag-icons is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.types:trusted-types is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:lit-element-state is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:echarts is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.hpcc-js:wasm is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:yargs is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:cliui is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:escalade is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:get-caller-file is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:require-directory is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:string-width is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:y18n is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:yargs-parser is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:strip-ansi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:wrap-ansi is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:emoji-regex is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:is-fullwidth-code-point is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:ansi-regex is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:ansi-styles is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:color-convert is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:color-name is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.mvnpm:codeblock is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.qomponent:qui-directory-tree is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:markdown-it is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:argparse is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:entities is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:linkify-it is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:mdurl is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:punycode.js is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:uc.micro is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.mvnpm:qomponent is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:ldrs is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm:es-module-shims is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.qomponent:qui-dot is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.viz-js:viz is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [bom/dev-ui] `<dependencyManagement>` entry org.mvnpm.at.panzoom:panzoom is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<exclusions>` on io.quarkus:quarkus-bom — exclusion support lands in a later slice; exclusions were dropped.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-devmode-test-utils is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.freemarker:freemarker is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.commonmark:commonmark is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.eclipse.jgit:org.eclipse.jgit is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.jboss.jdeparser:jdeparser is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry com.github.javaparser:javaparser-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-bootstrap-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-reactive-datasource-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-rest-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-war-launcher-runner is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-legacy-launcher is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry com.unboundid:unboundid-ldapsdk is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.wiremock:wiremock-standalone is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-integration-test-class-transformer is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-integration-test-shared-library is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-test-maven is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-security-test-utils is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-arc-test-supplement is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.quarkus:quarkus-arc-test-supplement-decorator is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.assertj:assertj-core is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.junit-pioneer:junit-pioneer is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.vineflower:vineflower is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.asciidoctor:asciidoctorj is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.htmlunit:htmlunit is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry jakarta.jms:jakarta.jms-api is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry com.github.davidmoten:subethasmtp is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.apache.artemis:artemis-server is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry org.apache.artemis:artemis-amqp-protocol is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] `<dependencyManagement>` entry io.smallrye.certs:smallrye-certificate-generator-junit5 is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [build-parent] Maven profile `format`: activation=activeByDefault+property=!no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [build-parent] Maven profile `validate`: activation=activeByDefault+property=no-format (no jk equivalent — replace with an explicit jk profile or feature); plugins=[formatter-maven-plugin,impsort-maven-plugin] (plugin mapping is not yet implemented).
- [build-parent] Maven profile `format-kotlin`: activation=activeByDefault+property=!no-format (no jk equivalent — replace with an explicit jk profile or feature)+file-existence (jk has no equivalent — refactor to a jk profile); plugins=[spotless-maven-plugin] (plugin mapping is not yet implemented).
- [build-parent] Maven profile `dokka`: activation=property=dokka (no jk equivalent — replace with an explicit jk profile or feature)+file-existence (jk has no equivalent — refactor to a jk profile); plugins=[dokka-maven-plugin,maven-jar-plugin] (plugin mapping is not yet implemented).
- [build-parent] Maven profile `eclipse`: activation=property=m2e.version (no jk equivalent — replace with an explicit jk profile or feature).
- [build-parent] Maven profile `test-coverage`: properties=[jacoco.agent.argLine] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest); plugins=[jacoco-maven-plugin] (plugin mapping is not yet implemented).
- [build-parent] Maven profile `api-check`: plugins=[build-helper-maven-plugin,revapi-maven-plugin] (plugin mapping is not yet implemented).
- [build-parent] Maven profile `Windows`: activation=os=Windows (use jk target predicates per dep); properties=[script.extension] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [build-parent] Maven profile `mac-m1`: activation=os=mac (use jk target predicates per dep); properties=[volume.access.modifier] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [build-parent] Maven profile `aarch64`: activation=os=? (use jk target predicates per dep); properties=[mssql.image] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [core] `<parent>` was referenced (io.quarkus:quarkus-build-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [core] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [test-framework] `<parent>` was referenced (io.quarkus:quarkus-build-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [test-framework] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [test-framework] `<plugin>maven-enforcer-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [extensions] `<parent>` was referenced (io.quarkus:quarkus-build-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [extensions] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [extensions] `<plugin>maven-enforcer-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [devtools] `<parent>` was referenced (io.quarkus:quarkus-build-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [devtools] `<modules>` block present but this import was run in single-POM mode. Re-run as `jk import pom.xml` from the project root to materialise a workspace.
- [integration-tests] `<parent>` was referenced (io.quarkus:quarkus-build-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [integration-tests] `<exclusions>` on io.quarkus:quarkus-bom-test — exclusion support lands in a later slice; exclusions were dropped.
- [integration-tests] `<dependencyManagement>` entry io.quarkus:quarkus-integration-test-class-transformer-deployment is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [integration-tests] `<dependencyManagement>` entry io.quarkus:quarkus-integration-test-class-transformer is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [integration-tests] `<dependencyManagement>` entry io.quarkus:quarkus-project-core-extension-codestarts is a version pin, not a BOM import. jk has no equivalent; the pin was dropped. Inline the version on the matching `<dependency>` instead.
- [integration-tests] Maven profile `test-modules`: activation=property=!no-test-modules (no jk equivalent — replace with an explicit jk profile or feature).
- [integration-tests] Maven profile `native`: activation=property=native (no jk equivalent — replace with an explicit jk profile or feature)+file-existence (jk has no equivalent — refactor to a jk profile); properties=[quarkus.native.enabled,quarkus.package.jar.enabled] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest); plugins=[maven-failsafe-plugin] (plugin mapping is not yet implemented).
- [integration-tests] Maven profile `native-ci`: activation=property=env.CI (no jk equivalent — replace with an explicit jk profile or feature); properties=[quarkus.native.builder-image.pull] (no jk equivalent — fold maven.compiler.* into project.jdk; drop the rest).
- [integration-tests] Maven profile `unbind-executions`: activation=file-existence (jk has no equivalent — refactor to a jk profile); plugins=[maven-source-plugin,maven-jar-plugin,maven-install-plugin] (plugin mapping is not yet implemented).
- [integration-tests] `<plugin>properties-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [integration-tests] `<plugin>maven-enforcer-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [docs] `<parent>` was referenced (io.quarkus:quarkus-build-parent:999-SNAPSHOT) but jk-import did not flatten its dependencyManagement / properties / build config. Run `mvn help:effective-pom` and re-import if any dependency versions are unresolved.
- [docs] `<dependency>` tools.jackson.core:jackson-databind has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [docs] `<dependency>` tools.jackson.dataformat:jackson-dataformat-yaml has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [docs] `<dependency>` io.quarkus:quarkus-devtools-registry-client has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [docs] `<exclusions>` on io.quarkus:quarkus-bootstrap-core — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<dependency>` io.quarkus:quarkus-bootstrap-core has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [docs] `<dependency>` org.asciidoctor:asciidoctorj has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [docs] `<dependency>` org.eclipse.jgit:org.eclipse.jgit has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [docs] `<dependency>` org.testcontainers:testcontainers has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [docs] `<dependency>` io.quarkus:quarkus-junit has no resolved `<version>`; jk wrote `=unresolved`. Run `mvn help:effective-pom` and re-import.
- [docs] `<exclusions>` on io.quarkus:quarkus-aesh-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-aesh-ssh-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-aesh-websocket-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-agroal-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-amazon-lambda-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-amazon-lambda-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-amazon-lambda-http-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-amazon-lambda-rest-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-amazon-lambda-xray-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-apache-httpclient-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-apicurio-registry-avro-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-apicurio-registry-json-schema-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-arc-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-assistant-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-avro-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-awt-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-azure-functions-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-azure-functions-http-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-cache-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-cache-kotlin-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-caffeine-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-config-yaml-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-confluent-registry-avro-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-confluent-registry-json-schema-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-container-image-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-container-image-buildpack-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-container-image-docker-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-container-image-docker-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-container-image-jib-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-container-image-openshift-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-container-image-podman-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-core-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-cyclonedx-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-cyclonedx-endpoint-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-data-hibernate-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-datasource-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-devjsonrpc-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-devmcp-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-devservices-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-devui-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-elasticsearch-java-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-elasticsearch-rest-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-elasticsearch-rest-client-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-elytron-security-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-elytron-security-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-elytron-security-jdbc-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-elytron-security-ldap-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-elytron-security-oauth2-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-elytron-security-properties-file-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-flyway-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-flyway-db2-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-flyway-mssql-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-flyway-mysql-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-flyway-oracle-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-flyway-postgresql-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-funqy-amazon-lambda-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-funqy-google-cloud-functions-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-funqy-http-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-funqy-knative-events-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-funqy-server-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-google-cloud-functions-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-google-cloud-functions-http-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-grpc-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-grpc-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hal-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-envers-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-orm-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-orm-panache-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-orm-panache-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-orm-panache-kotlin-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-orm-rest-data-panache-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-reactive-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-reactive-panache-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-reactive-panache-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-reactive-panache-kotlin-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-reactive-rest-data-panache-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-search-backend-elasticsearch-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-search-orm-elasticsearch-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-search-orm-outbox-polling-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-search-standalone-elasticsearch-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-hibernate-validator-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-http3-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-infinispan-cache-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-infinispan-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-info-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jackson-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jacoco-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jaxb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jaxp-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jdbc-db2-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jdbc-h2-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jdbc-mariadb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jdbc-mssql-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jdbc-mysql-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jdbc-oracle-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jdbc-postgresql-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jfr-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jlink-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jsonb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-jsonp-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-kafka-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-kafka-streams-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-keycloak-admin-client-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-keycloak-admin-rest-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-keycloak-admin-resteasy-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-keycloak-authorization-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-kind-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-kotlin-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-kubernetes-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-kubernetes-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-kubernetes-client-internal-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-kubernetes-config-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-kubernetes-service-binding-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-liquibase-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-liquibase-mongodb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-load-shedding-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-logging-gelf-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-logging-json-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-mailer-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-messaging-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-messaging-amqp-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-messaging-kafka-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-messaging-mqtt-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-messaging-pulsar-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-messaging-rabbitmq-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-micrometer-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-micrometer-opentelemetry-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-micrometer-registry-prometheus-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-minikube-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-modular-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-mongodb-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-mongodb-panache-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-mongodb-panache-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-mongodb-panache-kotlin-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-mongodb-rest-data-panache-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-mutiny-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-mutiny-reactive-streams-operators-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-narayana-jta-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-narayana-lra-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-narayana-stm-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-netty-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-observability-devservices-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-observation-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-oidc-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-oidc-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-oidc-client-graphql-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-oidc-client-registration-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-oidc-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-oidc-db-token-state-manager-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-oidc-redis-token-state-manager-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-oidc-token-propagation-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-openshift-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-openshift-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-opentelemetry-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-picocli-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-proxy-registry-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-quartz-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-qute-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-reactive-datasource-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-reactive-db2-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-reactive-mssql-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-reactive-mysql-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-reactive-oracle-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-reactive-pg-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-reactive-routes-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-reactive-transactions-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-redis-cache-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-redis-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-client-config-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-client-jackson-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-client-jaxb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-client-jaxrs-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-client-jsonb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-client-kotlin-serialization-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-client-oidc-filter-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-client-oidc-token-propagation-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-csrf-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-data-hibernate-json-types-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-data-panache-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-jackson-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-jackson-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-jaxb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-jsonb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-jsonb-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-kotlin-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-kotlin-serialization-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-kotlin-serialization-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-links-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-qute-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-rest-servlet-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-client-jackson-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-client-jaxb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-client-jsonb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-client-mutiny-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-client-oidc-filter-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-client-oidc-token-propagation-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-jackson-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-jaxb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-jsonb-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-links-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-multipart-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-mutiny-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-mutiny-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-qute-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-resteasy-server-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-scheduler-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-security-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-security-jpa-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-security-jpa-common-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-security-jpa-reactive-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-security-webauthn-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-signals-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-smallrye-context-propagation-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-smallrye-fault-tolerance-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-smallrye-graphql-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-smallrye-graphql-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-smallrye-health-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-smallrye-jwt-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-smallrye-jwt-build-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-smallrye-openapi-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-smallrye-reactive-streams-operators-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-smallrye-reactive-type-converters-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-smallrye-stork-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spiffe-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-boot-properties-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-cache-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-cloud-config-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-data-jpa-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-data-rest-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-di-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-scheduled-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-security-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-tx-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-web-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-web-rest-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-spring-web-resteasy-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-swagger-ui-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-tls-registry-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-undertow-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-vertx-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-vertx-graphql-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-vertx-http-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-vertx-kotlin-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-virtual-threads-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-web-dependency-locator-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-websockets-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-websockets-client-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] `<exclusions>` on io.quarkus:quarkus-websockets-next-deployment — exclusion support lands in a later slice; exclusions were dropped.
- [docs] Maven profile `unbind-skip-docs`: activation=property=skipDocs (no jk equivalent — replace with an explicit jk profile or feature); plugins=[maven-source-plugin,maven-jar-plugin,maven-install-plugin,graphviz-maven-plugin] (plugin mapping is not yet implemented).
- [docs] Maven profile `documentation-pdf`: activation=property=documentation-pdf (no jk equivalent — replace with an explicit jk profile or feature); plugins=[asciidoctor-maven-plugin,maven-assembly-plugin] (plugin mapping is not yet implemented).
- [docs] Maven profile `go-offline`: activation=property=go-offline (no jk equivalent — replace with an explicit jk profile or feature); repositories declared (move into the top-level `repositories` block).
- [docs] `<plugin>build-helper-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [docs] `<plugin>maven-resources-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [docs] `<plugin>quarkus-config-doc-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [docs] `<plugin>maven-surefire-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [docs] `<plugin>exec-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [docs] `<plugin>asciidoctor-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [docs] `<plugin>maven-assembly-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.
- [docs] `<plugin>graphviz-maven-plugin</plugin>` was not imported. Plugin-aware mappings (Spotless, JaCoCo, Spring Boot, ...) arrive in slice D.

