package dev.jkexamples.matrix;

/** Only the release variant compiles this class — extra-src is additive, not an override. */
public final class ReleaseStamp {
    private ReleaseStamp() {}

    public static final String STAMP = "release build";
}
